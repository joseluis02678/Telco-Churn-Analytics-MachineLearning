version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MODEL_PATH=/app/models/churn_logreg_pipeline.joblib
    volumes:
      - ./models:/app/models
    restart: unless-stopped
